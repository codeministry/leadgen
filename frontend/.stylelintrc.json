{
  "extends": "stylelint-config-standard",
  "rules": {
    "color-no-hex": true,
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["plugin", "theme", "utility", "variant", "custom-variant", "apply", "source"]
      }
    ]
  },
  "ignoreFiles": ["src/styles.css"]
}
